# 史莱姆树洞 · 待办事项

> 更新于 2026-08-25。这份文件只回答一个问题:**接下来做什么。**
> 历史进度看 `PROGRESS.md`,常驻决策看 `CLAUDE.md`。
>
> **状态图例**:⬜ 未开始 · 🟨 进行中 · ✅ 已完成 · ⏸️ 暂缓 · ❌ 本期不做

---

## 一、App 现在长什么样

界面结构在切片 8 之后整个换掉了 —— 从"导航栈里几个页面"变成"两页横滑 + 一个浮窗"。

```
UINavigationController
└── RootPagerViewController          横滑分页容器,底部小点跟随 offset
    ├── HomeViewController           page 0 · 农场(天空 + 浮岛 + 母鸡 + 鸟巢)
    │     点鸟巢 → present ComposeViewController(.overFullScreen)
    │                浮窗:农场截图虚化打底 + 奶白玻璃纸面 + 下蛋按钮
    └── SquareViewController         page 1 · 广场
          月份标题 + 绿胶囊周条(七格,蛋/日号)
          白卡:当天日记列表 ⇄ 原文(原地翻面,不 push)
          底部舞台:今天=母鸡 / 过去=当天情绪蛋
```

**关键机制**

| 机制 | 在哪 | 干什么 |
|---|---|---|
| `PagerPage` 协议 | `RootPagerViewController.swift` | 容器播报"谁是当前页",谁想听谁举手 |
| 三信号合成 | `HomeViewController` | `isOnScreen && isCurrentPage && !isCovered` 才跑帧循环 |
| 工厂闭包 | `HomeViewController.makeComposeViewController` | 组合根注入"造浮窗"的能力,首页不认识 ComposeVC |
| 截图虚化 | `Core/Snapshot.swift` | `drawHierarchy` + `CIGaussianBlur`,模糊半径可调 |

---

## 二、产品规则(已定稿,实现时按此)

这几条是切片 9 期间敲定的,和 `CLAUDE.md` 第 2 节的技术决策同等效力。

### 2.1 时间与封存

- **一天按自然零点划分**(直接用 `Calendar.startOfDay(for:)`)。曾评估过"凌晨 4 点换天"以照顾深夜写作,权衡后放弃 —— 少一条规则少一堆边界。以后想改再说。
- **过去的日子不可添加、不可修改、不可删除**。日子合上就是合上了。
- **当天可以添加、修改、删除**。今天还没封存,留一个反悔的余地。
- **不允许删除历史**,理由不只是一致性:用户最想删的正是难过的日子,而 `LowMoodStreakRule` / `MoodRecoverRule` 恰恰靠这些数据触发。**删除功能会静默关掉主动关心系统。** 真有隐私诉求就做"折叠"或 App 加锁,不做删除。

### 2.2 蛋是什么

**蛋 = 一天的封存物**,不是一篇日记。

```
一天里写 N 篇日记
      ↓ 到隔天时间,或用户手动孵
  母鸡下一颗蛋 = AI 总结 + 当天综合情绪
```

- 周条格子上的蛋、底部舞台上的蛋,都是同一颗 `DayEgg`
- ⚠️ **周条蛋的情绪要从"最后一篇的 emotion"改成 `DayEgg.emotion`**(AI 综合),现在还是旧逻辑
- **那天一篇没写就不下蛋**。空的日子就是空的,不为了填满而生成

### 2.3 史莱姆的去留(⬜ 未决)

**现在 App 里有两套生育隐喻在打架**:写一篇孵出一只史莱姆,而周条上是一天一颗蛋。用户写完看到一只史莱姆诞生,滑到广场却发现全是蛋,那只史莱姆再也找不到了。

**方向已确认**:一篇 = 往当天的巢里放一样东西,一天 = 一颗蛋。由此推出三件待办:

- 浮窗那个"下蛋"按钮**名字不对** —— 写一篇不是下蛋,母鸡到隔天才下蛋
- 写完的演出该改成"收进巢里",不是"孵出什么"。它同样能盖住 AI 调用的等待,套路不变只换形象
- `SlimeView` / `SlimeCell` / `slimeShape` 退休 —— **先留着别删**,以后给蛋做孵化动画时,那套 `CASpringAnimation` + `CAKeyframeAnimation` 的手法可以直接搬

**但数据层不动**(2026-08-25 决定)。单篇 `emotion` 是主动关心三条规则的燃料,和史莱姆长什么样没有关系:

- 删掉它 = 规则要按"天"重写,粒度从"篇"变"天"
- 而且触发至少延迟两天 —— 得等日蛋孵出来才有情绪数据,"刚写完难过的东西就被关心到"这个即时性会消失

等重新定义主动关心规则时,再一并决定要不要动数据层。

### 2.4 自动补蛋

iOS 做不到真正的定时后台执行,所以不是"到点自动下",而是 **"下次见面时补课"** —— 和主动关心的 `.appOpened` 同一个模式。

- 触发点:App 启动 + 滑到广场(有防重入守卫,重复触发无害)
- **一次只补最近的一个**。用这个替代退避策略:失败时每次只浪费一次调用,天然限流
- **失败就让它欠着**,不标记不假装,下次进来自然重试
- 两道守卫:`hatching: Set<Date>`(挡"上次还没回来")+ `lastAttempt`(挡"刚失败完立刻又来",30 秒)
- 守卫只在内存 —— 跨启动不需要成立,重启就是重试的信号

### 2.5 dayKey 固化(✅ 已完成)

**问题**:"属于哪一天"是 `createdAt` 推导出来的,不是存下来的。推导规则一变,所有历史数据的分组结果跟着变 —— 已经孵出来的蛋会和它那天的日记对不上,而且静默无提示。

换天时间定成自然零点之后,原来那条"规则会变"的理由消失了,但还剩两条:

1. **时区会变**。`startOfDay` 依赖 `Calendar` 的时区,用户出国一趟,历史日记的分组会集体位移一天
2. **推导结果被别的数据引用**。`DayEgg` 靠"哪一天"找它那天的日记 —— 一旦有东西引用了推导结果,推导结果就该固化

**语义**:一篇日记属于哪一天,由"**写的时候当地是几号**"决定,不由"现在看的时候人在哪"决定。所以固化不只是为了稳定,它本身就是更正确的定义。

**已落地**:`Post.dayKey`(Optional Date)+ `create` 时赋值 + `SlimeItem` 带字段 + 分组时 `$0.dayKey ?? startOfDay(createdAt)` 兜底。

**还欠**:⬜ 老数据回填一次(现在全是测试数据,有兜底能跑,**真上线前记得跑**):

```swift
let posts = repository.fetchAll().filter { $0.dayKey == nil }
posts.forEach { $0.dayKey = Calendar.current.startOfDay(for: $0.createdAt) }
CoreDataStack.shared.saveContext()
```

> 以后若真要做"用户自定义换天时间",配套规则:**设置变更延迟到下一个逻辑日生效**。立即生效会让新写的日记落进已封存的过去 —— 改设置那一刻,"现在属于哪一天"的答案会跳变。界面写一句"将从明天开始生效"即可。

---

## 三、切片 9:广场页重做

### 3.1 已完成 ✅

- ✅ `RootPagerViewController` 横滑容器 + dots 跟随 + `PagerPage` 协议
- ✅ 屏外帧循环停摆(三信号合成)
- ✅ `ComposeViewController` 改浮窗(present + 截图虚化 + 玻璃卡片 + `keyboardLayoutGuide`)
- ✅ `window.overrideUserInterfaceStyle = .light`(全 App 锁浅色,配色本来就没为深色设计过)
- ✅ `SquareViewModel` 按天分组 + 本周七格 + `selectedDay`
- ✅ `EggView` + 六张情绪蛋图 + `SlimeEmotion.eggAsset` 扩展
- ✅ `WeekStripView` 绿胶囊周条(七格 · 蛋/日号 · 选中高亮)
- ✅ `DiaryEntryCell` 列表行(情绪色点 + 原文前 14 字 + 时间)
- ✅ `DiaryDetailView` 原地翻原文,轻点任意处收起(带滚动中不误触的守卫)
- ✅ 详情页展示 AI 的 `reply`(旧待办顺手清掉)
- ✅ `NestStageView` 底部舞台(今天=母鸡 / 过去=情绪蛋 / 空白=素蛋)
- ✅ `SquareViewController` 实现 `PagerPage`,滑过来就重读

### 3.2 下一步:AI 总结一天(设计已定,代码未动)

按依赖顺序:

0. ✅ **`dayKey`** —— 见 2.5(回填还欠着,不阻塞)
1. ⬜ **`DayEgg` 实体**(`date` / `text` / `emotion` / `createdAt`,Codegen Manual)
   —— 不需要 `basedOnCount` / `basedOnLatest`,过去不可改则总结永不过时
2. ⬜ **`DayEggStore`** —— 值类型 `DayEggRecord` 对外;`save` 必须是 upsert;
   `eggs(from:to:)` 批量查(周条七天,别在循环里发查询)
3. ⬜ **`DayEggSummarizing` 协议 + prompt** —— 输入当天几篇原文+情绪,输出一句总结 + 综合情绪。
   `DeepSeekAIService` 实现。文案铁律照旧:不说教、不建议、短
4. ⬜ **`SquareViewModel` 补蛋编排** —— `@MainActor`、防重入守卫、`defer` 清理。
   ⚠️ 占位必须在 `await` **之前**:`@MainActor` 消除数据竞争,但每个 `await` 都是交错点
5. ⬜ **周条蛋的情绪改读 `DayEgg.emotion`**(见 2.2)
6. ⬜ **`NestStageView` 显示总结**,素蛋(未孵)加呼吸动画 + 点一下手动重试

### 3.3 再下一步:长按孵蛋

- ⬜ `HatchStage` 协议(`setHatchProgress` / `commitHatch` / `cancelHatch`)——
  **先定口子再换里子**:今天驱动 `transform`,以后换 Rive 只改实现
- ⬜ `UILongPressGestureRecognizer` + `minimumPressDuration = 0`(默认 0.5 秒会让人以为没反应),
  进度用 `CADisplayLink` 自己算
- ⏸️ 母鸡换 Rive —— 要在 Rive 编辑器里做一版**带 Number input 的画板**才能被进度驱动。
  现有 `Hen.riv` 的两台状态机没有对外 input(见 `RiveHenView` 注释),这是设计侧的前提

---

## 四、已知问题

按"会不会被用户撞到"排序。

| # | 问题 | 位置 |
|---|---|---|
| 1 | **聊天入口是死的** —— 浮窗里 `navigationController` 是 nil,可选链静默吞掉调用,点"聊聊"没反应 | `ComposeViewController.swift:354` |
| 2 | **没有草稿保存** —— "不能补写"这条规则要求草稿必须可靠:写到一半被杀掉,明天想补也补不回来 | `ComposeViewController` |
| 3 | **跨零点不换天** —— `today` 是 `init` 时算的,App 挂后台一夜再打开,周条不翻页、昨天仍显示母鸡,而且还能往昨天写 | `SquareViewModel` |
| 4 | **删除没有按规则限制** —— 长按菜单现在什么都能删,按 2.1 应该只允许删今天的 | `SquareViewController` 长按菜单 / `SquareViewModel.delete` |
| 5 | **主动关心气泡位置不对** —— `careBubble` 还在写日记浮窗里演,它本该是"打开 App 时"的事,应该挪到首页农场 | `ComposeViewController.presentCareIfNeeded` |
| 6 | `PostDetailViewController` 已无任何引用,死代码 | `ViewControllers/` |
| 7 | `fetchAll()` 全表加载 —— 数据量涨上来会线性变慢。做规则引擎第二批之前必须补 `fetch(from:to:)` / `fetchRecent(limit:)` | `PostRepository` |
| 8 | 蛋图没有 `@3x` 后缀,被当 1x 资源。不影响显示(`scaleAspectFit` 会缩),但尺寸计算和内存占用不合理 | `Assets/egg_*.png` |
| 9 | 数据量上来后记得给 `Post.createdAt` / `DayEgg.date` 开索引 —— 按时间范围查是这个 App 最高频的查询 | 数据模型 |

---

## 五、之后的候选

- ⬜ 第二批规则(个人基线统计)—— 依赖 #7 的按范围查询
- ⬜ 第三批规则(深夜连发 + 优先级抢占)
- ⬜ 自适应频控(按 `ignoredCount` 拉长冷却,只改引擎一处)
- ⬜ 心愿提醒(AIService 扩展 `hasWish` + `wishDate`)
- ⬜ AI 结构化输出补摘要 + 话题分类
- ⬜ 换天时间做成用户可设(配套 2.5 的延迟生效)
- ⬜ 空广场占位提示 / 揭晓可跳过 + 高频降级
- ⬜ 透明身体 + 彩虹液体填充
- ⬜ 爱心眼蛋、O 形嘴蛋两张备用图 → 彩虹态 / 连续高光的庆祝时刻
- ⏸️ 广场页背景换成 `SkyView` 打底(现在是纯色,和农场滑动切换时略有断层)

---

## 六、待确认项

- ⬜ **后端选型** —— 现为客户端直连 DeepSeek + `Secrets.plist`,**上线前必换**
- ⬜ 真机 / 开发者账号
- ⬜ 通知实现方式(主动关心第一版不用推送,推迟到心愿提醒再定)
- ⬜ 隐私处理(本地是否加密、上传 AI 前是否脱敏)
- ⬜ 主动关心的阈值与冷却时长(机制已定,参数需实测)
- ✅ 情绪粒度 —— 6 类锁死
- ✅ AI 服务选型 —— DeepSeek
